const CartWidget = () => {
    return(
        <div style={{display: "-ms-flexbox", color:"blue", justifyContent:"center", fontSize: "30px"}} >
            🎫
            <span style={{fontWeight: "bold", textDecorationColor: "Highlight"}}>1</span>
        </div>
    )    
}

export default CartWidget;